import 'package:flutter/material.dart';

class MapsDetailsView extends StatelessWidget {
  const MapsDetailsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
